# Copy this file to .env, fill in your own project's values from
# Supabase → Settings → API, and restart the dev server. On Vercel, add
# these same two variables under Project Settings → Environment Variables.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
